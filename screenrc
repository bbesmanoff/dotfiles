# Start without message
startup_message off

# Use ZSH
shell /bin/zsh

# Set the $TERM varaible
term screen-256color

# Set hardstatus bar
hardstatus alwayslastline
hardstatus string "%{rd}[%c:%s %D %M %d]%{-} %-w%{wr}(%n %t)%{-}%+w"

# Only display messages for one second
msgwait 1
